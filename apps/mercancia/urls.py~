from django.conf.urls import patterns, include, url

urlpatterns = patterns('apps.mercancia.views',
    # Examples:
    # url(r'^$', 'elpastillero.views.home', name='home'),
    url(r'^addMercancia/$', 'view_add_mercancia', name='vista_gregar_mercancia'),
)
